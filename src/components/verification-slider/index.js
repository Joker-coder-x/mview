import VerificationSlider from './src/verification-slider.vue';

VerificationSlider.install = function(Vue, options) {
    Vue.component(VerificationSlider.name, VerificationSlider);
}

export default VerificationSlider;