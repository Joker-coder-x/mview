export const THEME_TYPES = {
  default: {
    backgroundColor: "#fff",
    borderColor: "#ddd",
    textColor: "#5c6161"
  },
  primary: {
    backgroundColor: "#409EFF",
    borderColor: "#3798FE",
    textColor: "#fff"
  },
  success: {
    backgroundColor: "#67C23A",
    borderColor: "#50962E",
    textColor: "#fff"
  },
  info: {
    backgroundColor: "#909399",
    borderColor: "#809399",
    textColor: "#fff"
  },
  warning: {
    backgroundColor: "#E6A23C",
    borderColor: "#E49B2E",
    textColor: "#fff"
  },
  danger: {
    backgroundColor: "#F56C6C",
    borderColor: "#F45E5E",
    textColor: "#fff"
  }
};

// Contextmenu Events
export const CONTEXTMENU_EVENTS = {
  INSER_RELARION_NODE: "insertRelationNode",
  INTSER_CHILD_NODE: "insertChildNode",
  DELETE_NODE: "deleteNode"
};

// 默认配置
export const DEFAULT_CONFIG = {
  centralNodeFontSize: 16,
  otherNodeFontSize: 14,
  textColor: "#fff",
  centralNodeR: 40,
  otherNodeR: 30,
  centralNodeBgColor: "#409EFF",
  otherNodeBgColor: "#67C23A",
  textAnchor: "middle",
  lineTipWidth: 2,
  lineTipColor: "#8CC5FF",
  assistLineTipWidth: 10,
  assistLineTipColor: "transparent",
  lineTipRectWidth: 20,
  lineTipRectHeight: 20,
  lineTipRectBgColor: "#8CC5FF",
  lineTipRectRadius: 5,
  lineTipText: "?",
  lineTipFontSize: 10,
  initCenterR: 120,
  incrementR: 100,
  oneCircleMaxNodeNum: 9,
  allowScale: true
};
